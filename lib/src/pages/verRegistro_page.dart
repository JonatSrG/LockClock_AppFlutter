import 'package:flutter/material.dart';

class VerRegistroPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text(
          'Ver',
        ),
      ),
    );
  }
}
